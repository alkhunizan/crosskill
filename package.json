{
  "name": "crosskill-monorepo",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run --filter '@crosskill/core' build && bun run --filter 'crosskill' build",
    "build:web": "bun run --filter '@crosskill/core' build && bun run --filter '@crosskill/web' build",
    "test": "bun test",
    "typecheck": "bun run --filter '@crosskill/core' typecheck && bun run --filter 'crosskill' typecheck && bun run --filter '@crosskill/web' typecheck",
    "format": "prettier --write \"packages/*/src/**/*.ts\" \"apps/*/{src,app}/**/*.{ts,tsx}\"",
    "format:check": "prettier --check \"packages/*/src/**/*.ts\" \"apps/*/{src,app}/**/*.{ts,tsx}\""
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^20.12.7",
    "prettier": "^3.3.3",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/alkhunizan/crosskill.git"
  },
  "homepage": "https://crosskill.dev",
  "license": "MIT",
  "author": "Aziz Al-Khunizan <hello@azizme.com> (https://azizme.com)"
}
